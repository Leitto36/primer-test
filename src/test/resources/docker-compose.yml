version: '3'
services:
  web-app:
    build:
      context: .
      dockerfile: ../../../Dockerfile
    image: primer-demo:latest
    ports:
      - "8080:8080"
  mockserver:
    image: "jamesdbloom/mockserver:mockserver-5.9.0"
    ports:
      - "1080:1080"
